# Base API URL for your backend
# Example: http://localhost:5000/api/v1
VITE_API_BASE=

# Notes:
# - This app uses axios with credentials enabled (cookies) for refresh tokens.
# - Ensure your backend CORS allows the Vite dev origin and credentials.
#   Access-Control-Allow-Origin: http://localhost:5173 (or your dev URL)
#   Access-Control-Allow-Credentials: true
